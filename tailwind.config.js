/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  darkMode: false, // ⛔ Disable dark mode to avoid accidental overrides
  plugins: [],
};